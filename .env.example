DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB_NAME?schema=public"

# OpenAI wordt door n8n gebruikt, niet direct door Next.js,
# maar deze variabele is handig als je dezelfde waarde in n8n wil injecteren.
OPENAI_API_KEY="sk-..."

# Publieke basis-URL van je Next.js applicatie (zonder trailing slash),
# gebruikt door n8n om het callback-endpoint aan te roepen.
APP_BASE_URL="https://jouw-domein.nl"

# URL van de n8n-webhook die de ruwe tekst ontvangt.
N8N_INGEST_WEBHOOK_URL="https://jouw-n8n-host/webhook/ai-todo-ingest"

# Shared secret voor callbacks van n8n naar de backend.
N8N_CALLBACK_SECRET="vervang-door-een-lang-random-secret"

# AI Hub: n8n webhook voor o.a. dagnotitie. Gebruik N8N_AI_HUB_WEBHOOK (alleen server, aanbevolen) of NEXT_PUBLIC_N8N_AI_HUB_WEBHOOK.
# API POST /api/ai/daynote gebruikt deze waarde; bij ontbreken: "AI Hub-webhook is niet geconfigureerd."
N8N_AI_HUB_WEBHOOK="https://datadenkt.app.n8n.cloud/webhook/ai-hub"
# NEXT_PUBLIC_N8N_AI_HUB_WEBHOOK="https://datadenkt.app.n8n.cloud/webhook/ai-hub"

# Supabase (verplicht voor login en data). Zet in .env.local (nooit committen).
# Waarden vind je in Supabase: Project Settings → API → Project URL en anon public key.
NEXT_PUBLIC_SUPABASE_URL="https://jouw-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJ..."
# Service role alleen server-side voor admin user list (optioneel).
SUPABASE_SERVICE_ROLE_KEY="eyJ..."

